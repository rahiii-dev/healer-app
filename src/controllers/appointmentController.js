/**
 * @route   GET /api/appointments/:therapistId/date
 * @desc    list active slots available for a therapist in perticular date
 * @access  Private for Client only
 */
// export const get = asyncWrapper(async (req, res) => {