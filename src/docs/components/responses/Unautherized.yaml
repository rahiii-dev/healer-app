description: Unautherised User
content:
  application/json:
    schema:
      type: object
      properties:
        message:
          type: string
          example: "Unautherised. No token provided"
